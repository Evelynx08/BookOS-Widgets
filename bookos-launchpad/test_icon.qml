import QtQuick
import org.kde.kirigami as Kirigami
import QtQuick.Controls

ApplicationWindow {
    width: 200; height: 200; visible: true
    Kirigami.Icon {
        anchors.fill: parent
        source: "com.bookos.launchpad"
    }
}
